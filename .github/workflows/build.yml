name: Build

on:
    pull_request:
        branches:
            - main
    push:
        branches:
            - main

jobs:
    check_labels:
        uses: NordicSemiconductor/pc-nrfconnect-shared/.github/workflows/build.yml@main
        secrets: inherit